{
  "name": "coinpeas-static",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fetch-coins": "node scripts/fetch-coins.mjs src/coins.json",
    "deploy": "npm run fetch-coins && npm run build && wrangler deploy --message \"Manual deploy\""
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "wrangler": "^4.135.0"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.3.0"
  }
}
